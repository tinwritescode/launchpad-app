{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^db:generate", "^generateTypechain"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "launchpad-web#build": {
      "dependsOn": ["^build", "^generateTypechain", "^db:generate"],
      "outputs": [".next/**"]
    },
    "dashboard#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist"]
    },
    "launchpad-web#start": {
      "dependsOn": [
        "^launchpad-web#build",
        "^generateTypechain",
        "^db:generate"
      ],
      "outputs": [".next/**"]
    },
    "lint": {},
    "dev": {
      "dependsOn": [
        "addTSNoCheck",
        "deployContract",
        "generateTypechain",
        "runLocalNode",
        "^db:generate"
      ],
      "cache": false,
      "persistent": true
    },
    "deployContract": {
      "cache": false
    },
    "runLocalNode": {
      "cache": false
    },
    "generateTypechain": {},
    "addTSNoCheck": {
      "dependsOn": ["^generateTypechain"]
    },
    "db:generate": {
      "cache": false
    },
    "db:push": {
      "cache": false
    },
    "db:reset": {
      "cache": false
    },
    "db:seed": {
      "cache": false
    },
    "prisma:studio": {
      "cache": false
    }
  },
  "globalEnv": [
    "CHAIN_ID",
    "BLOCKCHAIN_RPC",
    "STAKING_CONTRACT_ADDRESS",
    "STAKING_TOKEN_ADDRESS",
    "REWARD_TOKEN_ADDRESS",
    "USDC_TOKEN_ADDRESS",
    "IDO_CONTRACT_ADDRESS",
    "IDO_TOKEN_ADDRESS",
    "DIVIDEND_CONTRACT_ADDRESS"
  ]
}
